---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import rawWaveTop from "../../assets/shapes/thorium-shape-3-wave-top.svg?raw";

interface Props {
  illustration: { src: ImageMetadata; alt?: string };
  class?: string;
  illustrationClass?: string;
}

const { illustration, class: className, illustrationClass } = Astro.props;
---

<div class:list={ ["cta-decoration", className] }>
  <div class="cta-wave cta-wave-top" aria-hidden="true">
    <Fragment set:html={ rawWaveTop } />
  </div>
  <Image
    class:list={ ["cta-decoration-illustration", illustrationClass] }
    src={ illustration.src }
    alt={ illustration.alt ?? "" }
  />
</div>
